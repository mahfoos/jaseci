- **Feature: MobUI target enhancements + jachammer reference app**: `@jac/mobui` now re-exports `KeyboardAvoidingView` and `ActivityIndicator`; the React Native target writes and imports a `__jacApiBase.js` shim so the native entry resolves the build-time API base URL before any bundle code runs; and a new `jac-mobui` guide/skill documents the cross-platform primitives, the no-HTML rule, and the `jac start --client react-native` build. Adds `jachammer` (`jac/examples/mobui/jachammer/`), a full mobile + web client for the hosted jacBuilder backend, as the worked reference example.
