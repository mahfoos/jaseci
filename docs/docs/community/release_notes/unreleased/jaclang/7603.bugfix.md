- **Reading anchors for classes a process does not define no longer floods the logs**: the serializer now warns at most once per unregistered class per process instead of once per anchor read, so services sharing a store with other apps keep genuine errors visible instead of drowning them in repeated identical warnings.
