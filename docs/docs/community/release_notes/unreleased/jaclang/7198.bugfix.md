- **Warm redeploys stop re-uploading unchanged bundles**: the PVC seeder skips objects already present (keys are sha256 of the content, so a present file is the payload), and all bundle archives are now deterministic (zeroed gzip timestamp, normalized tar members) so identical content actually keeps its digest across deploys - previously every pack got a fresh key and ~170MB re-shipped to the cluster on every redeploy.
