- **Scale: Shared pip cache survives pod uid changes**: the creating pod opens the PVC cache directories (0777, mutual-trust app volume) and a pod that still cannot write them falls back to a pod-local cache instead of exporting a directory pip silently refuses, which previously forced sdist rebuilds on every boot and crash-looped services after a channel-image republish.
