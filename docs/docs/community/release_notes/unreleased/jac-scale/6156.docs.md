- **Docs: MicroK8s-first local Kubernetes guidance and ingress-based URLs**: Local setup docs now recommend MicroK8s on Ubuntu -- with the full validated step list: `dns hostpath-storage` addons, `snap alias microk8s.kubectl kubectl`, and exporting `microk8s config > ~/.kube/config` (both required by the deploy tooling) -- while keeping Docker Desktop and Minikube as alternatives. All access URLs use the ingress NodePort default `http://localhost:30080` (previously stale `30001`), and the Kubernetes tutorial's configuration tables document the real `[scale.kubernetes]` `jac.toml` keys instead of removed env-style variables.
